<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.darkoandreev.webservicetest">
    <uses-permission android:name="android.permission.INTERNET" ></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dobrite_sasedi_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <supports-screens
            android:smallScreens="true"
            android:normalScreens="true"
            android:largeScreens="true"
            android:anyDensity="true" />

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClientDocuments"
            android:label="@string/client_doc">
        </activity>
        <activity
            android:name=".PartidiView"
            android:label="@string/partidi_string">
        </activity>

        <activity
            android:name=".PaymentsView"
            android:label="@string/plashtane_string">
        </activity>

        <activity
            android:name=".InvoicesView"
            android:label="Фактура">
        </activity>

    </application>

</manifest>